[pytest]
timeout = 60
asyncio_mode = auto
testpaths = tests
markers =
    integration: Integration tests
    concept(id): associate a test with a CONCEPT id
addopts = -m "not integration"
filterwarnings =
    ignore:.*exclude_args.*
